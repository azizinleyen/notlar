# Third-party components

The app uses Electron, React, TipTap, a SQLite binding, and other npm packages. Their licenses are included with the respective packages. Windows distributions include Electron and Chromium license texts as `LICENSE.electron.txt` and `LICENSES.chromium.html`.

`package-lock.json` records dependency versions. Review each dependency's license obligations before distributing a build.
